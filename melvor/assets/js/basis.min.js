/*!
 * @pixi/basis - v7.2.4
 * Compiled Thu, 06 Apr 2023 19:36:45 UTC
 *
 * @pixi/basis is licensed under the MIT License.
 * http://www.opensource.org/licenses/mit-license
 */
this.PIXI = this.PIXI || {};
var _pixi_basis = function(R, f, T, O) {
    "use strict";
    var y = (e => (e[e.cTFETC1 = 0] = "cTFETC1", e[e.cTFETC2 = 1] = "cTFETC2", e[e.cTFBC1 = 2] = "cTFBC1", e[e.cTFBC3 = 3] = "cTFBC3", e[e.cTFBC4 = 4] = "cTFBC4", e[e.cTFBC5 = 5] = "cTFBC5", e[e.cTFBC7 = 6] = "cTFBC7", e[e.cTFPVRTC1_4_RGB = 8] = "cTFPVRTC1_4_RGB", e[e.cTFPVRTC1_4_RGBA = 9] = "cTFPVRTC1_4_RGBA", e[e.cTFASTC_4x4 = 10] = "cTFASTC_4x4", e[e.cTFATC_RGB = 11] = "cTFATC_RGB", e[e.cTFATC_RGBA_INTERPOLATED_ALPHA = 12] = "cTFATC_RGBA_INTERPOLATED_ALPHA", e[e.cTFRGBA32 = 13] = "cTFRGBA32", e[e.cTFRGB565 = 14] = "cTFRGB565", e[e.cTFBGR565 = 15] = "cTFBGR565", e[e.cTFRGBA4444 = 16] = "cTFRGBA4444", e))(y || {});
    const w = {
            [0]: f.INTERNAL_FORMATS.COMPRESSED_RGB_ETC1_WEBGL,
            [2]: f.INTERNAL_FORMATS.COMPRESSED_RGB_S3TC_DXT1_EXT,
            [3]: f.INTERNAL_FORMATS.COMPRESSED_RGBA_S3TC_DXT5_EXT,
            [8]: f.INTERNAL_FORMATS.COMPRESSED_RGB_PVRTC_4BPPV1_IMG,
            [9]: f.INTERNAL_FORMATS.COMPRESSED_RGBA_PVRTC_4BPPV1_IMG,
            [11]: f.INTERNAL_FORMATS.COMPRESSED_RGB_ATC_WEBGL,
            [10]: f.INTERNAL_FORMATS.COMPRESSED_RGBA_ASTC_4x4_KHR
        },
        p = {
            [13]: T.TYPES.UNSIGNED_BYTE,
            [14]: T.TYPES.UNSIGNED_SHORT_5_6_5,
            [16]: T.TYPES.UNSIGNED_SHORT_4_4_4_4
        },
        L = Object.keys(w).map(e => Number(e)).reduce((e, t) => (e[w[t]] = t, e), {}),
        b = {
            [3]: !0,
            [9]: !0,
            [10]: !0
        };

    function x() {
        let e;
        const t = {
            init: r => self.BASIS ? (self.BASIS({
                wasmBinary: r.wasmSource
            }).then(s => {
                s.initializeBasis(), e = s, self.postMessage({
                    type: "init",
                    success: !0
                })
            }), null) : (console.warn("jsSource was not prepended?"), {
                type: "init",
                success: !1
            }),
            transcode(r) {
                const s = r.basisData,
                    a = e,
                    l = s,
                    n = new a.BasisFile(l),
                    B = n.getNumImages(),
                    c = n.getHasAlpha() ? r.rgbaFormat : r.rgbFormat,
                    _ = 14,
                    o = new Array(B);
                let g = !1;
                if (!n.startTranscoding()) return n.close(), n.delete(), {
                    type: "transcode",
                    requestID: r.requestID,
                    success: !1,
                    imageArray: null
                };
                for (let u = 0; u < B; u++) {
                    const P = n.getNumLevels(u),
                        I = {
                            imageID: u,
                            levelArray: new Array,
                            width: null,
                            height: null
                        };
                    for (let A = 0; A < P; A++) {
                        const h = g ? _ : c,
                            E = n.getImageWidth(u, A),
                            F = n.getImageHeight(u, A),
                            G = n.getImageTranscodedSizeInBytes(u, A, h),
                            M = E + 3 & -4,
                            v = F + 3 & -4;
                        A === 0 && (I.width = M, I.height = v);
                        const W = new Uint8Array(G);
                        if (!n.transcodeImage(W, u, A, h, !1, !1)) {
                            if (g) return console.error(`Basis failed to transcode image ${u}, level ${A}!`), {
                                type: "transcode",
                                requestID: r.requestID,
                                success: !1
                            };
                            console.warn(`Basis failed to transcode image ${u}, level ${A}! Retrying to an uncompressed texture format!`), u = -1, g = !0;
                            break
                        }
                        I.levelArray.push({
                            levelID: A,
                            levelWidth: E,
                            levelHeight: F,
                            levelBuffer: W
                        })
                    }
                    o[u] = I
                }
                return n.close(), n.delete(), {
                    type: "transcode",
                    requestID: r.requestID,
                    success: !0,
                    basisFormat: g ? _ : c,
                    imageArray: o
                }
            }
        };
        self.onmessage = r => {
            const s = r.data,
                a = t[s.type](s);
            a && self.postMessage(a)
        }
    }
    const d = class {
        constructor() {
            this.requests = {}, this.onMessage = e => {
                const t = e.data;
                if (t.type === "init") {
                    if (!t.success) throw new Error("BasisResource.TranscoderWorker failed to initialize.");
                    this.isInit = !0, this.onInit()
                } else if (t.type === "transcode") {
                    --this.load;
                    const r = t.requestID;
                    t.success ? this.requests[r].resolve(t) : this.requests[r].reject(), delete this.requests[r]
                }
            }, this.isInit = !1, this.load = 0, this.initPromise = new Promise(e => {
                this.onInit = e
            }), d.wasmSource || console.warn("resources.BasisResource.TranscoderWorker has not been given the transcoder WASM binary!"), this.worker = new Worker(d.workerURL), this.worker.onmessage = this.onMessage, this.worker.postMessage({
                type: "init",
                jsSource: d.jsSource,
                wasmSource: d.wasmSource
            })
        }
        static get workerURL() {
            if (!d._workerURL) {
                let e = x.toString();
                const t = e.indexOf("{"),
                    r = e.lastIndexOf("}");
                e = e.slice(t + 1, r), d.jsSource && (e = `${d.jsSource}
${e}`), d._workerURL = URL.createObjectURL(new Blob([e]))
            }
            return d._workerURL
        }
        initAsync() {
            return this.initPromise
        }
        async transcodeAsync(e, t, r) {
            ++this.load;
            const s = d._tempID++,
                a = new Promise((l, n) => {
                    this.requests[s] = {
                        resolve: l,
                        reject: n
                    }
                });
            return this.worker.postMessage({
                requestID: s,
                basisData: e,
                rgbaFormat: t,
                rgbFormat: r,
                type: "transcode"
            }), a
        }
        static loadTranscoder(e, t) {
            const r = fetch(e).then(a => a.text()).then(a => {
                    d.jsSource = a
                }),
                s = fetch(t).then(a => a.arrayBuffer()).then(a => {
                    d.wasmSource = a
                });
            return Promise.all([r, s]).then(a => (this._onTranscoderInitializedResolve(), a))
        }
        static setTranscoder(e, t) {
            d.jsSource = e, d.wasmSource = t
        }
    };
    let S = d;
    S.onTranscoderInitialized = new Promise(e => {
        d._onTranscoderInitializedResolve = e
    }), S._tempID = 0;
    const i = class {
        static async transcode(e) {
            let t;
            return typeof Worker != "undefined" && i.TranscoderWorker.wasmSource ? t = await i.transcodeAsync(e) : t = i.transcodeSync(e), t
        }
        static async transcodeAsync(e) {
            !i.defaultRGBAFormat && !i.defaultRGBFormat && i.autoDetectFormats();
            const t = i.workerPool;
            let r = 268435456,
                s = null;
            for (let _ = 0, o = t.length; _ < o; _++) t[_].load < r && (s = t[_], r = s.load);
            s || (s = new S, t.push(s)), await s.initAsync();
            const a = await s.transcodeAsync(new Uint8Array(e), i.defaultRGBAFormat.basisFormat, i.defaultRGBFormat.basisFormat),
                l = a.basisFormat,
                n = a.imageArray,
                B = l > 12;
            let c;
            if (B) c = n.map(_ => new T.BufferResource(new Uint16Array(_.levelArray[0].levelBuffer.buffer), {
                width: _.width,
                height: _.height
            }));
            else {
                const _ = w[a.basisFormat];
                c = new Array(n.length);
                for (let o = 0, g = n.length; o < g; o++) c[o] = new f.CompressedTextureResource(null, {
                    format: _,
                    width: n[o].width,
                    height: n[o].height,
                    levelBuffers: n[o].levelArray,
                    levels: n[o].levelArray.length
                })
            }
            return c.basisFormat = l, c
        }
        static transcodeSync(e) {
            !i.defaultRGBAFormat && !i.defaultRGBFormat && i.autoDetectFormats();
            const t = i.basisBinding,
                r = new Uint8Array(e),
                s = new t.BasisFile(r),
                a = s.getNumImages(),
                l = s.getHasAlpha() ? i.defaultRGBAFormat.basisFormat : i.defaultRGBFormat.basisFormat,
                n = y.cTFRGB565,
                B = new Array(a);
            let c = i.fallbackMode;
            if (!s.startTranscoding()) return s.close(), s.delete(), null;
            for (let o = 0; o < a; o++) {
                const g = c ? 1 : s.getNumLevels(o),
                    u = s.getImageWidth(o, 0),
                    P = s.getImageHeight(o, 0),
                    I = u + 3 & -4,
                    A = P + 3 & -4,
                    h = new Array(g);
                for (let F = 0; F < g; F++) {
                    const G = s.getImageWidth(o, F),
                        M = s.getImageHeight(o, F),
                        v = s.getImageTranscodedSizeInBytes(o, 0, c ? n : l);
                    if (h[F] = {
                            levelID: F,
                            levelBuffer: new Uint8Array(v),
                            levelWidth: G,
                            levelHeight: M
                        }, !s.transcodeImage(h[F].levelBuffer, o, 0, c ? n : l, !1, !1)) {
                        if (c) break;
                        o = -1, c = !0;
                        continue
                    }
                }
                let E;
                c ? E = new T.BufferResource(new Uint16Array(h[0].levelBuffer.buffer), {
                    width: u,
                    height: P
                }) : E = new f.CompressedTextureResource(null, {
                    format: w[l],
                    width: I,
                    height: A,
                    levelBuffers: h,
                    levels: g
                }), B[o] = E
            }
            s.close(), s.delete();
            const _ = B;
            return _.basisFormat = c ? n : l, _
        }
        static autoDetectFormats(e) {
            if (!e) {
                const r = T.settings.ADAPTER.createCanvas().getContext("webgl");
                if (!r) {
                    console.error("WebGL not available for BASIS transcoding. Silently failing.");
                    return
                }
                e = {
                    s3tc: r.getExtension("WEBGL_compressed_texture_s3tc"),
                    s3tc_sRGB: r.getExtension("WEBGL_compressed_texture_s3tc_srgb"),
                    astc: r.getExtension("WEBGL_compressed_texture_astc"),
                    etc: r.getExtension("WEBGL_compressed_texture_etc"),
                    etc1: r.getExtension("WEBGL_compressed_texture_etc1"),
                    pvrtc: r.getExtension("WEBGL_compressed_texture_pvrtc") || r.getExtension("WEBKIT_WEBGL_compressed_texture_pvrtc"),
                    atc: r.getExtension("WEBGL_compressed_texture_atc")
                }
            }
            const t = {};
            for (const r in e) {
                const s = e[r];
                !s || Object.assign(t, Object.getPrototypeOf(s))
            }
            for (let r = 0; r < 2; r++) {
                const s = !!r;
                let a, l;
                for (const n in t)
                    if (a = t[n], l = L[a], l !== void 0 && (s && b[l] || !s && !b[l])) break;
                a ? i[s ? "defaultRGBAFormat" : "defaultRGBFormat"] = {
                    textureFormat: a,
                    basisFormat: l
                } : (i[s ? "defaultRGBAFormat" : "defaultRGBFormat"] = {
                    textureFormat: T.TYPES.UNSIGNED_SHORT_5_6_5,
                    basisFormat: y.cTFRGB565
                }, i.fallbackMode = !0)
            }
        }
        static bindTranscoder(e) {
            i.basisBinding = e
        }
        static loadTranscoder(e, t) {
            return i.TranscoderWorker.loadTranscoder(e, t)
        }
        static setTranscoder(e, t) {
            i.TranscoderWorker.setTranscoder(e, t)
        }
        static get TRANSCODER_WORKER_POOL_LIMIT() {
            return this.workerPool.length || 1
        }
        static set TRANSCODER_WORKER_POOL_LIMIT(e) {
            for (let t = this.workerPool.length; t < e; t++) this.workerPool[t] = new S, this.workerPool[t].initAsync()
        }
    };
    let m = i;
    m.fallbackMode = !1, m.workerPool = [], m.TranscoderWorker = S;
    const C = {
        extension: {
            type: T.ExtensionType.DetectionParser,
            priority: 3
        },
        test: async () => !!(m.basisBinding && m.TranscoderWorker.wasmSource),
        add: async e => [...e, "basis"],
        remove: async e => e.filter(t => t !== "basis")
    };
    T.extensions.add(C);
    var j = Object.defineProperty,
        D = Object.getOwnPropertySymbols,
        U = Object.prototype.hasOwnProperty,
        H = Object.prototype.propertyIsEnumerable,
        N = (e, t, r) => t in e ? j(e, t, {
            enumerable: !0,
            configurable: !0,
            writable: !0,
            value: r
        }) : e[t] = r,
        q = (e, t) => {
            for (var r in t || (t = {})) U.call(t, r) && N(e, r, t[r]);
            if (D)
                for (var r of D(t)) H.call(t, r) && N(e, r, t[r]);
            return e
        };
    const k = {
        extension: {
            type: T.ExtensionType.LoadParser,
            priority: O.LoaderParserPriority.High
        },
        name: "loadBasis",
        test(e) {
            return O.checkExtension(e, ".basis")
        },
        async load(e, t, r) {
            await S.onTranscoderInitialized;
            const s = await (await T.settings.ADAPTER.fetch(e)).arrayBuffer(),
                a = await m.transcode(s),
                l = p[a.basisFormat],
                n = a.basisFormat !== y.cTFRGBA32 ? T.FORMATS.RGB : T.FORMATS.RGBA,
                B = a.map(c => {
                    const _ = new T.BaseTexture(c, q({
                        mipmap: c instanceof f.CompressedTextureResource && c.levels > 1 ? T.MIPMAP_MODES.ON_MANUAL : T.MIPMAP_MODES.OFF,
                        alphaMode: T.ALPHA_MODES.NO_PREMULTIPLIED_ALPHA,
                        type: l,
                        format: n
                    }, t.data));
                    return O.createTexture(_, r, e)
                });
            return B.length === 1 ? B[0] : B
        },
        unload(e) {
            Array.isArray(e) ? e.forEach(t => t.destroy(!0)) : e.destroy(!0)
        }
    };
    return T.extensions.add(k), R.BASIS_FORMATS = y, R.BASIS_FORMATS_ALPHA = b, R.BASIS_FORMAT_TO_INTERNAL_FORMAT = w, R.BASIS_FORMAT_TO_TYPE = p, R.BasisParser = m, R.INTERNAL_FORMAT_TO_BASIS_FORMAT = L, R.TranscoderWorker = S, R.detectBasis = C, R.loadBasis = k, Object.defineProperty(R, "__esModule", {
        value: !0
    }), R
}({}, PIXI, PIXI, PIXI);
Object.assign(this.PIXI, _pixi_basis);
//# sourceMappingURL=basis.min.js.map